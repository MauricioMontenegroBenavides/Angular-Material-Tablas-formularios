// Descargar Git 
// Crear cuenta en Git-Hub
// git config --global user.name "Tu nombre"
// git config --global user.email "maur1025@hotmail.com"
// git init (inicializar el repositprio)
// git add . (traer nuestro archivo)
// git commit -m "se agrega archivo index"
// Despues es subir este archivo  local a la nube de GITHUB
// git remote add origin https://github.com/MauricioMontenegroBenavides/angular-basicos.git
// git branch -M main
// git push -u origin main
// 

// Si se desea tener mantener un codigo sin ls futuras ediciones se debe utilizar los siguientes comandos 
// git tag -a v0.1.0 -m "Fin seccion 4"
// git push --tags(TOMAR UNA SOLA VERSION)


// npm install reconstruye todos los modulos de node (Esto funciona cuando descargamos de github y no tenemos los modulos)